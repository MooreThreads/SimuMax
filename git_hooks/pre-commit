#!/bin/sh

echo "🔍 Running compare_all_results.py..."
cd develop/tests
python3 compare_all_results.py

if [ $? -ne 0 ]; then
    echo "❌ Performance checks failed. Commit aborted."
    exit 1
fi

echo "✅ All checks passed! Ready to commit."

